# 高精度智能电子注射器管控系统 - 完整产品需求规格书

## 1. 技术栈（严格遵循）
- 后端：Python 3.10+，FastAPI
- ebSocket：FastAPI 原生 WebSocket（starlette.websockets）
- 数据库：SQLite（使用 better-sqlite3 库）
- 实时通信：ws 库（WebSocket 服务端），前端原生 WebSocket
- 硬件通信：serialport（串口模拟：无真实硬件时，后端自动生成模拟压力与药量数据）
- 前端：React (函数组件 + Hooks)，使用 antd 作为 UI 组件库，recharts 绘制图表
- 构建与部署：前端打包后，FastAPI 直接挂载静态文件（app.mount）

- 启动：pip install -r requirements.txt && python main.py，然后局域网内浏览器访问 http://<服务器IP>:8000


## 2. 核心功能（必须全部实现，不允许遗漏）
### 2.1 注射控制
- 四种注射模式：持续输注、间歇输注、按需推注、自定义曲线给药
- 参数设置：速率、剂量、时间、间隔
- 启动/停止注射：向连接的注射器发送指令（通过串口，若无硬件则模拟指令成功）
- 实时进度显示：所有已连接前端均能看到注射进度、剩余时间、当前速率
- 提醒同步：注射开始、完成时，所有终端弹出通知（使用 WebSocket 广播）

### 2.2 安全监测与紧急中断
- 后端定时读取注射器回传的压力、药量数据（或按配置的模拟数据）
- 三级报警阈值可在系统设置中修改（预警值、警告值、紧急停注值）
- 任意阈值被触发时：
  - 立即下发“紧急中断”指令到注射器（或模拟中断）
  - 将报警信息记录到数据库
  - 通过 WebSocket 向所有终端广播报警事件，前端弹出醒目提示并闪烁
- 报警事件列表，支持按时间、级别筛选

### 2.3 用药数据管理
- 每次注射结束后自动保存记录：操作员、时间、时长、剂量、模式、备注
- 剂量推荐算法：取最近5次注射剂量的加权平均值作为推荐值
- 历史记录查询：支持按日期范围、模式、剂量范围筛选，分页显示
- 统计图表：
  - 日/周/月剂量趋势柱状图
  - 报警次数按级别饼图
- 数据导出：Excel 格式

### 2.4 硬件通信与设备管理
- 后端启动后自动检测可用串口，提供设备连接/断开接口
- 若检测不到硬件，自动切换为“模拟模式”，使用随机漫步数据模拟压力和药量
- 设备注册：录入注射器唯一ID，绑定到系统
- 设备状态实时展示（在线/离线/工作中）

### 2.5 用户与权限管理
- 两种角色：管理员（可配置系统、查看所有数据）、操作员（仅进行注射操作）
- 基于用户名密码的简单登录，密码用 bcrypt 加密存储
- 登录后返回 JWT，前端所有请求带 token

- 操作日志：记录时间、用户、IP、操作内容

### 2.6 局域网多终端协同
- 系统为一个局域网 Web 服务器，所有终端通过浏览器访问
- 同一时间允许多终端登录，实时数据通过 WebSocket 同步
- 前端界面响应式布局，适配桌面和平板
- 注射器操作加互斥锁：当注射正在进行时，非启动终端的“停止注射”按钮才可用，避免并发冲突
- 终端断开连接后重连，能自动恢复最新状态

## 3. 非功能性需求
- 代码结构清晰：后端按路由、控制器、服务、模型分层；前端组件化
- 所有代码必须包含中文注释
- 数据库表结构在首次运行时自动初始化（使用 SQL 脚本）
- 系统预估总代码量（不含 node_modules）：≥3500 行
- 提供 `README.md`，说明如何安装依赖、启动、访问

## 4. 对 AI 的最高优先级指令

### 4.1 任务流程要求
请严格根据以上需求，一次性生成该系统的全部项目文件。
工作流程：
1. 在对话中先以 Plan Mode 输出完整的文件清单和技术架构说明（不需要我确认，输出后直接继续）。
2. 立即创建所有文件并写入完整代码，中间不要停顿，不要提出任何问题，不要要求澄清。
3. 生成的代码必须是完整的、可直接运行的真实实现，不能使用 TODO 或占位符
4. 所有函数、接口、WebSocket 事件、数据库表均需完整实现。

### 4.2 代码拟人风格要求（极其重要）
你生成的代码必须避免“完美 AI 风格”，要模拟一名有三年工作经验、略带个性的全栈程序员的编码习惯。以下是强制要求：

1.  **命名风格**
    *   禁止使用教科书式的长命名（如 `getCurrentInjectionStatus`）。
    *   可以使用拼音与英文混用（如 `yao_liang_remain`）、无厘头缩写（如 `tmpX`）、中文拼音直接作变量名（如 `yalijiance`）。
    *   前端状态变量可以使用口语化简称，如 `st`(status),等。
    *   函数名可以随意一些：`getData()`, `doShot()`, `startIt()`。

2.  **注释风格**
    *   **绝对不要**写统一、规整、中英对照的官方注释。
    *   注释的语言可以中英文混杂。

3.  **代码结构与逻辑**
    *   **允许适度不规范**：比如有一段功能全写在一个大函数里，而不是拆成 10 个小函数。可以存在一些重复的代码片段（如重复的错误处理）。
    *   不要刻意追求设计模式、高内聚低耦合。代码要像“边写边改出来的”，不是设计好的。
    *   在关键的注射中断、报警广播逻辑里，可以故意用一两个 `setTimeout` 来代替更好的异步处理。
    *   前端组件里可以混用类组件和函数组件（虽然我们用 Hooks，但可以故意留一个 Class Component）。
    *   可以存在无伤大雅的冗余变量，或赋值后并未使用的变量（请确保这不会影响程序运行）。


5.  **全局一致性**
    *   整个项目的拟人风格要保持一致，就像一个程序员写的。

请务必遵守上述风格要求，这比技术实现本身更重要。不要试图解释或反驳这些要求，直接生成符合要求的代码